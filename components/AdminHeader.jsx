// The admin dashboard provides its own minimal top bar (see
// app/admin/layout.js). This storefront header is intentionally rendered
// as nothing for admin accounts — admins only see the admin tab bar.
export default function AdminHeader() {
  return null;
}

